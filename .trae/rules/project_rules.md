【强制代码质量规则，每次编辑必须遵守】
1. 你输出的所有代码，必须保证：**无语法错误、无格式错误、无缩进错误、无括号/引号不匹配、无未定义变量、无类型错误、无导入错误、无 Lint/TS 报错**。
2. 不允许出现任何会导致编辑器红波浪线、编译报错、运行时报错的代码。
3. 只允许存在一种错误：**因用户需求描述不明确导致的业务逻辑错误**；除此之外的任何错误（语法、格式、类型、导入、拼写、符号匹配等）都必须为 0。
4. 输出前必须做一次自检：补全缺失符号、修正缩进、补全导入、保证 TypeScript 类型完全正确、保证可直接保存不报错。
5. 技术栈：NestJS + TypeScript + Prisma + MySQL；Vue3 + TypeScript + Element Plus。严格遵循项目现有代码风格、命名、目录结构。
6. 每次修改只输出正确可运行代码，不要带任何解释性错误代码。